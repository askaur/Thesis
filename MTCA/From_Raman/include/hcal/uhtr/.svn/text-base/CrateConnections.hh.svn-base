#ifndef hcal_uhtr_crateconnections
#define hcal_uhtr_crateconnections 1

/* Compatible code is used in hcalUpgrade and the InterchangePoint is used to align the two if needed. */
#include "uhal/ConnectionManager.hpp"

namespace hcal {
  namespace uhtr {
    
    uhal::ConnectionManager* getConnectionManager();
    uhal::ConnectionManager* getConnectionManager(const std::string& connections_file);
    void resetConnectionManager();


  }
}

#endif // hcal_uhtr_crateconnections
